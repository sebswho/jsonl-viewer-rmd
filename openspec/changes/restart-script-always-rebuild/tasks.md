# 实施任务

- [x] 1. RED：增加行为测试，证明已有 `.next/BUILD_ID` 时仍必须执行生产构建。
- [x] 2. GREEN：以最小改动移除条件构建，让每次重启都执行 `pnpm build`。
- [x] 3. RED：增加重启顺序测试，要求旧服务停止并确认端口释放后才构建和启动。
- [x] 4. GREEN：实现有超时的端口释放等待，不改变无关进程保护策略。
- [x] 5. REFACTOR：扫描未使用代码、重复逻辑和多余命名，保持最小 diff，并运行完整测试。
- [x] 6. RED：用含空格路径的临时 `.cmd` 构建器复现 Windows `EINVAL`，并验证构建失败能显示底层启动错误。
- [x] 7. GREEN：通过 `cmd.exe /d /c call` 执行 pnpm 构建，保持参数边界并改进错误传播。
- [x] 8. 再次运行完整测试和类型检查，确认新增 Windows 进程边界没有回归。
- [x] 9. 连续两次执行 `start-3201.cmd`，验证入口及时退出、第一次 PID 被第二次关闭、新 PID 监听 `0.0.0.0:3201`、两次构建均刷新且 HTTP 可访问。
- [x] 10. 汇总测试结果、净代码行变化和运行日志位置，完成交付。
